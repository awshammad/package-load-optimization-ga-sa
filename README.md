# package-load-optimization-ga-sa
Python package-to-vehicle assignment optimizer with a CustomTkinter GUI, using Genetic Algorithm (GA) and Simulated Annealing (SA) to minimize route distance with capacity/priority penalties (includes report).
